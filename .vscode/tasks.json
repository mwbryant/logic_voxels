{
	"version": "2.0.0",
	"tasks": [
        {
            "label": "cargo run",
            "type": "shell",
            "command": "~/.cargo/bin/cargo fmt && ~/.cargo/bin/cargo run", // note: full path to the cargo
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
    ]
}